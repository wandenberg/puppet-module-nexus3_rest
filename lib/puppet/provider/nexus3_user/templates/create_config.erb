def user = new org.sonatype.nexus.security.user.User()
user.setUserId('<%= resource[:name] %>')

<%= Puppet::Provider::Nexus3Utils.render_template(templates_folder, 'set_attributes.erb', resource) %>

security.securitySystem.addUser(user, '<%= resource[:password] %>')
